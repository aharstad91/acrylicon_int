# Ralph Progress Log
Started: Fri Feb 27 09:00:21 CET 2026
---

## Story 1: Core Web Vitals — Norwegian homepage /no/ [COMPLETE]
**Result:** PASS — LCP 297ms, CLS 0.00
**Key learnings:**
- LCP element is a hero `<video>` (2.5MB MP4) — streams via HTTP 206, loads fast
- 301 redirects from `/norway/` to `/no/` on video + images — stale DB references, adds ~19ms to LCP resource
- Large PNG (882KB) should be WebP (~200KB) — stale `/norway/` path too
- image2.jpg (589KB) and haslumslide2.jpg (426KB) could be WebP for ~75% savings
- LCP video missing `fetchpriority="high"` (currently Low priority)
- All 4 render-blocking CSS files load in <7ms via H2 — not a bottleneck
- Third-party scripts (FB 518KB, GTM 348KB, etc.) all deferred — ~1MB total but no FCP/LCP impact
- No console errors, clean execution
- Servebolt TTFB is excellent (32ms)

## Story 2: Core Web Vitals — English homepage / [COMPLETE]
**Result:** PASS — LCP 354ms, CLS 0.00
**Key learnings:**
- LCP element is same hero `<video>` (2.5MB MP4), no redirect on EN (direct path)
- EN homepage has JS error: "Swiper is not defined" in scripts.js — Swiper lib not loaded on pages without slider block, but init code runs anyway
- CRITICAL FINDING: Servebolt WebP content negotiation works on EN but NOT on NO
  - image2.jpg: EN serves 63KB WebP, NO serves 589KB JPEG
  - Large PNG: EN serves 52KB WebP, NO serves 882KB PNG
  - haslumslide2: EN serves 30KB WebP, NO serves 426KB JPEG
  - Total savings on EN vs NO: ~1,752KB — likely because NO's `/norway/` → `/no/` 301 redirects bypass WebP negotiation
- haslumskole-1024x526.jpg (2016 image) not WebP-converted on either site — only 39KB so low priority
- GSAP render-blocking from CDN (25ms) but estimated savings: 0ms
- Third-party scripts same profile as NO: ~1,078KB, all deferred
- Fixing the `/norway/` DB references on NO is highest priority — would enable WebP and save ~1.7MB

## Story 3: Core Web Vitals — Product page, reference page, office page [COMPLETE]
**Result:** PASS — All 3 pages: LCP 251–288ms, CLS 0.00
**Key learnings:**
- Product page LCP 251ms (text H2), Reference 288ms (image), Office 258ms (text P) — all ~10x under 2500ms threshold
- TTFB consistently excellent: 20–30ms across all page types
- Reference page `image-86.jpg` is 1,031KB JPEG not served as WebP — WebP variant likely missing on disk
- Product page JPEGs (106KB, 122KB, 146KB) also not WebP-negotiated — older uploads (2016/2021) likely lack WebP variants
- Reference page LCP image `image2.jpg` correctly served as WebP (63KB) — content negotiation works for newer uploads
- Swiper conditionally loads only on pages with slider block: 245.8KB JSDelivr on reference vs 72.8KB on product/office — confirmed working
- No console errors on any inner page (unlike EN homepage's "Swiper is not defined")
- No `/norway/` → `/no/` redirect issue on inner pages — only affects homepage media paths
- All third-party scripts deferred, no FCP/LCP impact, total main thread time < 35ms per page

## Story 4: Render-blocking resource audit [COMPLETE]
**Result:** FAIL — 4 issues found, but actual performance impact is minimal
**Key learnings:**
- Only 4 CSS files are render-blocking: block-library (16.7KB gz), style.css (1.5KB), fonts.css (0.3KB), tailwind.css (5.7KB)
- All load in <12ms via H2 multiplexing — Chrome estimates 0ms savings from fixing
- `wp-block-library/style.min.css` is 113.6KB raw (16.7KB compressed) — largest render-blocking resource, most styles unused by ACF+Tailwind theme
- Fix: `add_filter('should_load_separate_core_block_assets', '__return_true')` — WP 5.8+ feature to load per-block styles
- GSAP (71KB) in footer without defer — free optimization to add defer attribute
- wp-emoji inline script (5.8KB) loads on every page — unnecessary for corporate site
- WordPress Global Styles inline block (9KB) mostly unused CSS custom properties
- gravity.css, block-panels.css already use media="print" onload swap pattern — well optimized
- Swiper correctly conditional: 169KB CSS+JS only on slider-block pages (verified on 4 pages)
- jQuery properly deferred, analytics properly delayed (3.5s/interaction)
- Cache headers excellent: max-age=31536000 on all static assets

## Story 5: Image optimization audit [COMPLETE]
**Result:** FAIL — Systemic WebP generation gap, multiple oversized JPEGs
**Key learnings:**
- CORRECTION to Story 2: WebP content negotiation NOW works on NO site too (haslumslide2.jpg=30KB WebP, image2.jpg=63KB WebP) — the `/norway/` redirect issue from Story 2 may have been a caching artifact or has since been fixed
- Servebolt WebP content negotiation works perfectly via `Vary: Accept` + Apache, BUT only when a `.webp` file exists on disk
- Only ~3 images from Feb 2025 have WebP variants (image2.jpg, haslumslide2.jpg, imagehhh8.jpg, imagefda3.jpg, image-98-1.jpg)
- ALL older uploads (2016–2024) and ALL `.jpeg` extension files have NO WebP variant — served as original JPEG regardless
- `.jpeg` extension images (b375329e, natoipn, 9720950f, IMG_29571) consistently lack WebP — possible bug where WebP generator only processes `.jpg` not `.jpeg`
- Worst offenders: image-86.jpg (1,031KB), image-98.jpg (985KB) on single reference page — uncompressed/low-compression JPEGs at 679×908px
- Reference archive loads ALL 100 posts on single page — 100 thumbnail images, ~15-30MB total if all scrolled
- Homepage has 2 images with `fetchpriority="high"` — should only be 1 (the LCP element)
- GOOD: All images have width+height (no CLS), all have srcset, 97/100 archive images have loading="lazy"
- GOOD: Cache headers (max-age=604800) on all images
- #1 priority fix: Bulk WebP generation for all existing uploads, especially `.jpeg` files
- #2 priority fix: Re-encode oversized JPEGs (image-86, image-98) at quality 80

## Story 6: Third-party script and font loading audit [COMPLETE]
**Result:** PASS — All third-party scripts deferred, fonts optimally loaded
**Key learnings:**
- GTM + Byggfakta loaded via excellent delayed pattern: setTimeout(3500ms) or first user interaction (mouseover/touchstart/scroll/keydown)
- All GTM-triggered scripts (Facebook 518KB, LinkedIn 54KB, Readpeak 4KB) consequently deferred too
- Total third-party: ~1,078KB transfer, only ~40ms main thread time, ZERO FCP/LCP impact
- Facebook Pixel is heaviest third-party at 518KB transfer + 21ms main thread — consider server-side Conversions API
- Only 1 font loaded per page: Soehne Buch (33KB woff2), preloaded, font-display:swap — best practices
- Unused fonts (Kursiv, Mono) declared in CSS but browser correctly skips download
- `<meta name="theme-color">` present (#253761)
- Preconnect hints for cdn.jsdelivr.net and googletagmanager.com — good
- Minor: stats.docu.info has 301 redirect `/analytics/js` → `/analytics/js/` (trailing slash)
- Minor: Redundant dns-prefetch for cdn.jsdelivr.net (preconnect already covers it)
- Minor: jQuery Migrate (5KB) still loaded in production — unnecessary
